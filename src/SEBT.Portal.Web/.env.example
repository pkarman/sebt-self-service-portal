# SEBT Portal Environment Configuration

# Next.js automatically exposes NEXT_PUBLIC_* variables to the browser and converted to process.env.NEXT_PUBLIC_* in next.config.ts

# State Configuration (dc|co)
# Determines which design tokens are loaded and which theme is applied
STATE=dc

BASE_URL=http://localhost:3000
